# Role & Objective
You are an expert Frontend Architect specializing in React, Next.js (App Router), TypeScript, and Tailwind CSS. 

Your task is to take my provided static HTML/CSS design files and refactor them into a modular, production-ready landing page component for a high-end AI Automation Agency. The implementation must convert the interactive interactive centerpiece into a state-managed, highly-optimized React engine.

---

# Next.js & Architecture Requirements

1. **Component Structure**: 
   - Write clean, modular React code using TypeScript interfaces for all data layers.
   - Use the `"use client"` directive at the top for components handling runtime animation and layout states.
   
2. **Icons & Assets**:
   - Convert all inline SVG strings or image nodes from the raw HTML files into native React components or clean self-contained constants. 
   - Ensure all image elements leverage Next.js optimization strategies or inline semantic primitives where applicable.

3. **Styling & Theme Integration**:
   - Map all custom embedded raw CSS configurations directly into pure Tailwind CSS utility classes.
   - Maintain the premium dark aesthetic (`#030712` / `bg-slate-950`) with electric blue and emerald-green accent glows.

---

# Kinetic Orbit System State Specifications

You must manage the geometric orbit network and dynamic central fulfillment panel using robust React states (`useState`, `useEffect`):

1. **Geometric Circular Network Layout**:
   - Implement the exact circular layout path (`440px x 440px`) using pure Tailwind and mathematical inline CSS variables (`--angle`) mapping to increments of 72 degrees / 1.256 radians for exactly 5 nodes.
   - This ensures all client profile avatars are uniformly separated across screen sizes and never bunch up, overflow, or clip.

2. **The 5 Core Capability Services**:
   - Setup a structured TypeScript array object storing data for these 5 distinct client/service configurations:
     1. **Business Presence (Website / APP)**
     2. **Agent Development for Booking, Appointment**
     3. **Shopify / TikTok / Instagram Shop**
     4. **Digital Marketing SEO, AEO**
     5. **Content Automation Handling**

3. **Fulfillment Engine React State Matrix**:
   - **Active Index State**: Track the currently processing client node (0 to 4).
   - **Active Step State**: Track the 3 sequential milestone rows (0 to 2) within that specific project execution timeline (e.g., *01 Discuss Scope*, *02 Build Infrastructure*, *03 Launch Production*).
   - **Completed Registry State**: Maintain an array of indices tracking finalized projects. When a project finishes its 3rd milestone, its avatar node index must be pushed into this registry.
   - **Visual State Locks**: Avatars inside the completed array must permanently render with a clean green success boundary (`border-2 border-emerald-500 shadow-emerald-500/20`) directly on their designated orbit coordinate. They **never** fly away, unmount, or drop frame rate.

4. **Central Glassmorphic Card Constraints**:
   - Firmly containerized (`w-[320px]` to `w-[340px]`) to ensure no layout breaking, padding overflows, or word wrapping issues.
   - Incorporate a **Prominent Center Success Star Indicator Badge** (`bg-amber-500/10 text-amber-400`) within the header section.
   - **The Metrics Footer Row**: Display a static "99.8% Efficiency Rate" count right above **exactly 4 shining gold metrics star indicators** (`text-amber-400`).

---

# Execution & Delivery Instructions
- Provide a single, complete `HeroSection.tsx` file containing fully-typed React code.
- Ensure the state timing loop runs gracefully using a standard clean-up architecture within a React `useEffect` block to avoid memory leaks or duplicate execution ticks.
- Do not introduce bloated external animation wrapper libraries—achieve all state-driven transitions and timing bars cleanly through native Tailwind CSS utility primitives.